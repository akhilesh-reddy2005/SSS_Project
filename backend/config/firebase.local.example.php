<?php

return [
    // Optional: set explicitly if autodetection is not enough.
    'project_id' => 'your-firebase-project-id',

    // Absolute path or backend-relative path to your Firebase Admin service account JSON.
    // Example: __DIR__ . '/firebase-service-account.json'
    'credentials_path' => __DIR__ . '/firebase-service-account.json',

    // Firebase Web API key from project settings.
    'web_api_key' => 'your-firebase-web-api-key',
];
